# 0.3.1 — Android compatibility test candidate

- Select Flutter's documented conservative Android renderer.
- Preserve existing courses, controls, progress, purchase handling and ad
  behavior; no new visual/gameplay or monetization feature in this build.

## Purpose and limits

The configuration candidate has a clean analyzer and 176 passing tests,
with three pre-existing optional skips. Those results are not an Android
device run. This build tests one renderer hypothesis; it is not a confirmed
fix for the reported startup failure.

Packaging version is **0.3.1+5**, package `com.tsorostudios.fliptide`, using the
existing permanent upload key. No Play availability is implied until a
separately approved internal release is verified in Console. Do not promote
to closed Alpha/production without affected-device validation.

## Internal tester notes — draft, not published

Android renderer compatibility test update. Please check launch, existing
progress, play/retry and background/resume. No gameplay changes. Report the
version and Android version if startup still fails. Do not uninstall or
clear app storage to try this update.
