# gh-pages-trim — throwaway evidence branch (directive 2026-09-02i, item 1)

Pruned web build of Fliptide (private branch `next/payload-trim`, scripts/prune_web.sh).
NOT served by GitHub Pages (Pages stays on the packaged build). Measurements in
VERIFY_skwasm.txt / VERIFY_canvaskit.txt were taken by serving this exact tree locally
with gzip at 10 Mbps / 40 ms RTT, cache off, all third-party hosts blocked.
Delete this branch after the trim PR merges.
